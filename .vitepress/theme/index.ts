import DefaultTheme from "vitepress/theme";

import "react-notion-x/src/styles.css"; // (required) core styles shared by all of react-notion-x
import "prismjs/themes/prism-tomorrow.css"; // code block theme

export default DefaultTheme;
